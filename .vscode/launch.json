{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Attach to Edge",
            "port": 9222,
            "request": "attach",
            "type": "msedge",
            "webRoot": "${workspaceFolder}"
        },
        {
            "name": "Open AI Guidebook in Browser",
            "type": "chrome",
            "request": "launch",
            "file": "${workspaceFolder}/bror.html"
        }
    ]
}